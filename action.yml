name: 'setup bundletool'
description: 'setup bundletool of android'
author: 'amyu'

inputs:
  version:
    description: 'Bundletool version'
    required: false
    default: '1.15.1'

runs:
  using: 'node16'
  main: 'dist/index.js'